import { Box } from "@mui/material";

const ShoppingCart = () => {
  return (
    <Box sx={{ backgroundColor: "var(--secondary-bg)", height: "100vh" }}>
      Hello from shopping cart
    </Box>
  );
};

export default ShoppingCart;
